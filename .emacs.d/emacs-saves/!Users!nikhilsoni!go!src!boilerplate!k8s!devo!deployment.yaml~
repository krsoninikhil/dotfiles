apiVersion: apps/v1
kind: Deployment
metadata:
  name: payoutapi
  namespace: payoutapi
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: payoutapi
      version: v1.0.0
  template:
    metadata:
      labels:
        app: payoutapi
        version: v1.0.0
    spec:
      imagePullSecrets:
        - name: regsecret
      containers:
        - name: payoutapi
          image: docker-registry.cashfree.com/payoutapi:latest
          volumeMounts:
            - name: config-volume
              mountPath: /config
          env:
            - name: PAYOUTAPICONF
              value: /config/config.yml
          resources:
            limits:
              memory: ".5Gi"
              cpu: "700m"
            requests:
              memory: "256Mi"
              cpu: "350m"
          ports:
            - name: app
              containerPort: 9005
      volumes:
        - name: config-volume
          configMap:
            name: payoutapi