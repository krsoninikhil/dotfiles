#[no_std]
#[no_main]

use core::panic::PanicInfo;
use phil_opp_rust_os::{exit_qemu, serial_println};

#[no_mangle]
pub extern "C" fn _start() -> ! {
    serial_println!("ok");

    unsafe { exit_qemu(); }
    loop {}
}
