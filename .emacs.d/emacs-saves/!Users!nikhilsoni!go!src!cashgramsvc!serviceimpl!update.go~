package serviceimpl

import (
	"cashgramsvc/logger"
	"context"
)

func (r *Cashgram) UpdateStatus(ctx context.Context, params *models.UpdateStatus) error {
	if params.CFID != 0 {
	} else if params.CashgramID != 0 && params.AccountID != 0 {
	} else {
		logger.Error(ctx, "update Cashgram", "Invalid condition parameters", logger.Field("params", params))
	}
}
