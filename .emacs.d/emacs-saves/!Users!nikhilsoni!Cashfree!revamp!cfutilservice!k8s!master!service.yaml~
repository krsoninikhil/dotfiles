apiVersion: v1
kind: Service
metadata:
  name: cfutilservice
  namespace: cfutilservice
spec:
  selector:
    app: cfutilservice
  ports:
    - port: 80
      targetPort: 9188
      protocol: TCP
      name: cfutilservice
