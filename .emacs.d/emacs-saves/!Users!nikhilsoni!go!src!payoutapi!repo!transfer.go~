package repo

import (
	"context"
	"payoutapi/domains"
)

// Transfer defines repo interface for Transfer
type Transfer interface {
	Create(ctx context.Context, p domains.AddBeneRequest) (*domains.CreateIntResponse, error)
	Show(ctx context.Context, beneID string) (*domains.ShowBeneIntResponse, error)
}
