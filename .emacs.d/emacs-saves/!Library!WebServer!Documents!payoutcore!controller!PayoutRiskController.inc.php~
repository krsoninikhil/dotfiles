<?php

class PayoutRiskController {

    public static velocityCheck($dbHandle, $mongoHandle, $mcHandle, $cfTransferId, $details) {
        $accountDailyLimit = static::getAccountDailyLimit($dbHandle
    }
}

?>
