package models

// RequestHeaders from request meta data
type RequestHeaders struct {
	XRequestID string `json:"X-Request-Id"`
}

// ContextKey context key
type ContextKey string

// key for setting request header to context
const ReqHeadKey = "RequestHeaders"
